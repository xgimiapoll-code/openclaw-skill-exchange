services:
  - type: web
    name: openclaw-skill-exchange
    runtime: docker
    plan: free
    envVars:
      - key: PORT
        value: "8100"
      - key: MARKET_DB_PATH
        value: /app/data/market.db
    disk:
      name: market-data
      mountPath: /app/data
      sizeGB: 1
